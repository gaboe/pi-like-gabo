{
  "name": "subagents",
  "private": true,
  "type": "module",
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "check": "tsc --noEmit -p .",
    "prepare": "effect-tsgo patch && npm run build:native",
    "test": "node --test --experimental-strip-types *.test.ts"
  },
  "dependencies": {
    "effect": "^4.0.0-beta.99"
  },
  "devDependencies": {
    "@effect/tsgo": "0.24.2",
    "typescript": "^7.0.2"
  }
}
